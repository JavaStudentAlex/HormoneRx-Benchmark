# On-prem SER sidecar deps. Install into a venv the sound agent points at
# (default: /home/alex/.cache/hormonerx-ser-venv, override with SER_PYTHON).
#   python3 -m venv <venv> && <venv>/bin/pip install -r backend/ser/requirements.txt
torch
transformers
numpy
